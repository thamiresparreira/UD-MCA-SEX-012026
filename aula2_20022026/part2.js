// Parte 2 - Array de Objetos
let produtoMaquiagem = {
  nome: "Batom Líquido Retro Matte - MAC",
  cor: "High Drama",
  preco: 161.00,
  estoque: 689
};

// RESPONDENDO AS PERGUNTAS
// (Utilizado o console.log apenas para a visualização dos resultados e validar a execução da atividade)
// A) Como acessar o nome do objeto?
console.log(produtoMaquiagem.nome); // Este é o seguinte console.log para acessar o nome do objeto.

//B) Como acessar o preço usando colchetes?
console.log(produtoMaquiagem["preco"]); // Desta forma.

//C) Atualize o estoque para 80
produtoMaquiagem.estoque = 80; // Estoque atualizado.

//D) Imprima todas as propriedades no console
console.log(produtoMaquiagem); // Impressão feita.